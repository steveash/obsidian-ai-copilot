# Changelog

## Unreleased

### Added
- Structured patch workflow: preview, apply transaction set, rollback command.
- Richer persistent chat panel with inline source citations + click-to-open note navigation.
- Retrieval query metadata constraints/boosts (`folder:`, `tag:`, `before:`, `after:`, `link:`).
- Background indexing queue + diagnostics command output.
- Expanded integration/e2e-style coverage for retrieval/artifact flow and queue behavior.
- Security hardening toggles: remote model gate, redaction toggle, prompt size cap, strict config validation.
- Release automation improvements via `typecheck`, `release:check`, and CI typecheck step.
